# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

#the following four is exact search, according to Location, Status, Objectid, and Address
GET     /searchByLocation                                 controllers.Application.searchByLocation
GET     /searchByStatus                                   controllers.Application.searchByStatus
GET     /searchByObjectid                                 controllers.Application.searchByObjectid
GET     /searchByAddress                                  controllers.Application.searchByAddress

#return the nearest truck for set users(users' info should include longitude and latitude)
GET     /nearByFoodTruck                                  controllers.Application.getNearByFoodTruck
